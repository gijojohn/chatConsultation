version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: out    # Changed from .next to out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
